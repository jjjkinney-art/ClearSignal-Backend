━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STAGE 7A  ·  REVEAL FILE
DO NOT OPEN until both evaluators have submitted score sheets.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SYSTEM IDENTITIES
─────────────────
System A = Claude Sonnet (claude.ai — no special prompting)
System B = GPT-4o (chat.openai.com — no special prompting)
System C = ClearSignal (Q-First architecture, 6-matrix runtime)

The responses from Systems A and B were collected by submitting
each question verbatim to the respective web interfaces with no
additional context or instruction.

ClearSignal (System C) responses were generated by the
production backend at clearsignal-backend-dlsc.onrender.com,
runtime version 6-matrix, commit 82a1897ac113.  Each response
includes the Q-First Direct Answer, Key Drivers, Bull/Bear
thesis, Valuation, Macro sensitivity, Key risks, and Conclusion.

RESPONSE COLLECTION DATE
─────────────────────────
System C (ClearSignal): 2026-06-07
System A (Claude Sonnet): [date to be filled at collection]
System B (GPT-4o):       [date to be filled at collection]

NOTE ON VALIDITY
─────────────────
The system assignment (A/B/C) is fixed across all 10 questions.
Evaluators who completed scoring before opening this file
produced blind scores.  Any evaluator who opened this file
before completing their score sheet should be flagged as a
validity threat.

AFTER REVEAL
─────────────
1. Collect both evaluator score sheets.
2. Compute per-criterion averages for each system.
3. Compute inter-rater reliability (Pearson r or Cohen's
   weighted kappa) across evaluators for each system.
4. Any r < 0.7 on a criterion indicates rubric ambiguity —
   review that criterion's scoring notes before drawing
   conclusions from it.
5. Report results as:
   ClearSignal vs. baseline LLMs:
   · Mean advantage/disadvantage on each criterion
   · Win rate (questions where C outscored A and B)
   · Criterion where C most consistently wins
   · Criterion where C most consistently loses
