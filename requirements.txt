# ─────────────────────────────────────────────────────────────────────────────
# PINNED production dependency set (Sprint 0 reproducibility).
#
# Source of truth for humans: requirements.in (loose ranges). Deploys install
# from THIS file (exact pins) so the artifact you test is the artifact you ship.
# Regenerate after editing requirements.in, ideally via `pip-compile` (pip-tools)
# in a Python 3.11 environment, then verify a clean install + `import app.main`.
#
# Versions below are the known-good set verified with the test suite. The four
# runtime-only packages behind off-by-default flags (asyncpg / PyJWT / stripe /
# cryptography) are pinned to current stable releases and are install-verified by
# CI on Python 3.11 (they are not exercised locally under the default flags).
# ─────────────────────────────────────────────────────────────────────────────

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.128.8
starlette==0.49.3
uvicorn[standard]==0.39.0

# ── LLM client ────────────────────────────────────────────────────────────────
openai==2.32.0

# ── Data validation & settings ────────────────────────────────────────────────
pydantic==2.13.3
pydantic-settings==2.11.0

# ── HTTP / environment ────────────────────────────────────────────────────────
requests==2.32.5
python-dotenv==1.2.1

# ── Persistence (Phase 9A) ────────────────────────────────────────────────────
sqlalchemy==2.0.50
asyncpg==0.30.0          # PostgreSQL async driver (production)
aiosqlite==0.22.1        # SQLite async driver (dev / tests)
alembic==1.16.5          # Schema migrations

# ── HTTP client (async, used for JWKS fetch in RS256 path) ───────────────────
httpx==0.28.1

# ── JWT verification (Phase 16 · Slice 3) ────────────────────────────────────
PyJWT==2.10.1
cryptography==44.0.1     # pulled by PyJWT[cryptography]; pinned for reproducibility

# ── Stripe billing (Phase 17 · Slice 3) ──────────────────────────────────────
stripe==12.5.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.4.2
pytest-asyncio==1.2.0    # async test support
pytest-forked==1.6.0     # optional per-test forking. NOTE: not sufficient on its
                         # own for the legacy hermetic tests (see conftest.py /
                         # docs/API.md). Run one process PER FILE for fully
                         # deterministic RUN results.
